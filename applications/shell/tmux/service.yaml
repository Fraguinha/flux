apiVersion: v1
kind: Service
metadata:
  name: shell-tmux
  namespace: shell
  labels:
    app: shell-tmux
spec:
  type: ClusterIP
  ports:
    - port: 2222
      targetPort: ssh
      protocol: TCP
      name: ssh
  selector:
    app: shell-tmux
