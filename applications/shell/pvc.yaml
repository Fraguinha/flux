apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shell-data
  namespace: shell
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
  storageClassName: longhorn
