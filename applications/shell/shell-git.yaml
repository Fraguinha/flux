apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: shell-git
  namespace: flux-system
spec:
  interval: 1m
  ref:
    branch: main
  secretRef:
    name: git-credentials
  url: https://github.com/Fraguinha/shell
