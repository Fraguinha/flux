apiVersion: v1
kind: Service
metadata:
  name: pi-agent-tmux
  namespace: pi-agent
  labels:
    app: pi-agent-tmux
spec:
  type: ClusterIP
  ports:
    - port: 2222
      targetPort: ssh
      protocol: TCP
      name: ssh
  selector:
    app: pi-agent-tmux
