apiVersion: v1
kind: Service
metadata:
  name: pi-agent
  namespace: pi-agent
  labels:
    app: pi-agent
spec:
  type: ClusterIP
  ports:
    - port: 7681
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: pi-agent
