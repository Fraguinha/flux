apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: home-assistant-flux
spec:
  statefulSet:
    pod:
      nodeSelector:
        kubernetes.io/hostname: fraguinha
      tailscaleContainer:
        securityContext:
          capabilities:
            add:
              - NET_ADMIN
