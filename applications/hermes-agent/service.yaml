apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  namespace: hermes-agent
  labels:
    app: hermes-agent
spec:
  type: ClusterIP
  ports:
    - port: 9119
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: hermes-agent
