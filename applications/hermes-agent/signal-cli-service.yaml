apiVersion: v1
kind: Service
metadata:
  name: signal-cli
  namespace: hermes-agent
spec:
  selector:
    app: signal-cli
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
