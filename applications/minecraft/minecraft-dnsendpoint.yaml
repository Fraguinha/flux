apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: minecraft
  namespace: minecraft
spec:
  endpoints:
    - dnsName: minecraft.fraguinha.com
      recordType: A
      recordTTL: 300
      targets:
        - ${PUBLIC_IP_FRAGUINHA}
