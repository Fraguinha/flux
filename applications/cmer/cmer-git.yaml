apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cmer-git
  namespace: flux-system
spec:
  interval: 1m
  ref:
    branch: main
  secretRef:
    name: git-credentials
  url: https://github.com/fraguinha/cmer
