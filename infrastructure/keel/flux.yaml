apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-keel
  namespace: flux-system
spec:
  interval: 1m
  timeout: 30m
  path: ./infrastructure/keel
  prune: true
  wait: true
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: keel
      namespace: flux-system
  sourceRef:
    kind: GitRepository
    name: flux-system
