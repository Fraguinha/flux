apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: pi
  namespace: ssh
spec:
  endpoints:
    - dnsName: pi.fraguinha.com
      recordType: A
      recordTTL: 300
      targets:
        - ${PUBLIC_IP_FRAGUINHA}
