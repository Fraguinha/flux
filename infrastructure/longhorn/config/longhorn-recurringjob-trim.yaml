apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: filesystem-trim
  namespace: longhorn-system
spec:
  cron: "0 3 * * *"
  task: filesystem-trim
  groups:
    - default
  concurrency: 1
  retain: 0
