# k3s node configuration snapshot

Verbatim copies of each node's k3s systemd unit/env files, captured 2026-09-19.
Not reconciled by Flux — k3s has to be running before Flux exists, so this
directory is a manual audit baseline, not applied automatically. Token values
are redacted.
